import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  categoryContainer: {
    marginBottom: 20,
  },
  categoryTitle: {
    fontSize: 22,
    fontWeight: 'bold',
    marginBottom: 10,
    marginLeft: 10,
  },
  productList: {
    paddingBottom: 10,
  },
});

export default styles;
